<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color (soft white with transparency) -->
    <solid android:color="#FFFFFFFF" />

    <!-- Rounded corners -->
    <corners android:radius="16dp" />

    <!-- Subtle border -->
    <stroke
        android:width="1dp"
        android:color="#22000000" />

    <!-- Inner shadow/elevation effect -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>